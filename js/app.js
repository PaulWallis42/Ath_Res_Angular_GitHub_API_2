//  Creating our Angular application 'GitUserSearch' and pulling in the ngResource module as an external dependency.
//  Then we are assigning it all do the variable 'githubUserSearch'
var githubUserSearch = angular.module('GitUserSearch', ['ngResource']);
